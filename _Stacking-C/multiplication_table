#include <stdio.h>

int main(void)
{
//구구단 만들기

	for (int x = 2; x <= 9; x++)
	{
		printf("%d단 계산\n", x);
		for (int y = 1; y <= 9; y++)
		{
			printf(" %d X %d = %d\n", x, y, x * y);
		}
	}
  
  return 0;
 }
